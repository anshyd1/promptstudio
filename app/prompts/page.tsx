import type{Metadata}from'next';import PageHero from'@/components/PageHero';import PromptCard from'@/components/PromptCard';import{promptItems,categories}from'@/lib/prompts';export const metadata:Metadata={title:'Free AI Prompt Library for Images, Video & Marketing',description:'Browse 36 free customizable prompts with visual examples for Gemini, ChatGPT, Midjourney, Sora and Veo.'};export default function Prompts(){const sorted=[...promptItems].sort((a,b)=>Number(b.featured)-Number(a.featured));return <><PageHero eyebrow="FREE PROMPT LIBRARY" title="Prompts that make decisions." intro="Professional visual prompt systems with editable variables, model guidance and real examples."/><section className="library"><div className="chips">{categories.map(c=><span key={c}>{c}</span>)}</div><div className="galleryGrid">{sorted.map(p=><PromptCard p={p} key={p.id}/>)}</div></section></>}
