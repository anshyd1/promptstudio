import type{Metadata}from'next';import{notFound}from'next/navigation';import PageHero from'@/components/PageHero';import ToolBuilder from'@/components/ToolBuilder';import{tools}from'@/lib/tools';export function generateStaticParams(){return tools.map(t=>({slug:t.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,t=tools.find(x=>x.slug===slug);return t?{title:t.title,description:t.desc}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,t=tools.find(x=>x.slug===slug);if(!t)notFound();return <><PageHero eyebrow={`FREE ${t.type.toUpperCase()} TOOL`} title={t.title+'.'} intro={t.desc}/><section className="toolPage"><ToolBuilder type={t.type}/><div className="how"><span>WORKFLOW</span><h2>Make each decision explicit.</h2><p>Use the generated structure as a starting brief. Replace placeholders, remove anything irrelevant and review outputs against the real objective.</p></div></section></>}
